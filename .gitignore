node_modules/
dist/
coverage/
*.log
.DS_Store
.fm-evidence/
# A per-tool pointer at AGENTS.md, which is where this project's agent notes
# actually live. Untracked: it is local tooling config, not part of the product.
CLAUDE.md
